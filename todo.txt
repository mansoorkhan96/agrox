Post Like,
Farmer Tag {show posts by tag}Link On side

categories clickable with pagination

search for product, blog, forum, consultant

index($id = null) {
    if(id) {
        items = useritems

        return
    }

    items = allitems

    return
}

dont bring discussions in blog


Pros: {
    Cart Library,
    Helpers,
    Provider table seperation,
    Policies,
    ACL Management,
    Dynamic Pagination,
    Ajax only Requests,
    
}